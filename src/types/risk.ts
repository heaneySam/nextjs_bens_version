// src/types/risk.ts
export interface RiskClass {
  id: number; // Assuming backend provides an ID
  name: string; // e.g., "Credit & Political Risk"
  slug: string; // e.g., "credit-political-risk"
  // Add other relevant fields if provided by the API
} 